import UIKit

var label = UILabel(frame: CGRect(x:0, y:0, width: 300, height: 100))

label.text = "Hello Playground"

label.font = UIFont(descriptor: UIFontDescriptor(name: "Arial-Black", size: 20), size: 20)

label.textColor = UIColor.greenColor()